let dia = "domingo";

// para testar a condição (SEMPRE DENTRO DE ()) usamos nossa palavra reservada IF e aplicamos a condição: se a variável acima se cumpre.
if (dia == "domingo"){

// caso a condição se cumpra (DIA SER == DOMINGO), abaixo podemos imprimir o que desejamos fazer (SEMPRE DENTRO DE {})
    console.log("Vou para a praia")
}





// // aqui utilizamos o operador lógico && (AND) para que explicitamente as condições se cumpram ("ensolarado" e "domingo") para executar o bloce de código que vai estar entre as chaves
// let dia = "domingo";
// if (clima == "ensolarado" && dia == "domingo");

// // caso as condições da linha acima se cumpram temos o código para executar o comando esperado:
