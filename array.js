// no array abaixo, atribuímos uma variável e entre [] atribuímos seus valores.
// cada valor tem sua posição no array. "Vinicius" está na posição 0; 22 está na posição 1; 1.80 está na posição 2 e assim por diante. Todos os valores são separados por ,.
let pessoa = ["Vinicius", 22, 1.80, false, ["Música", "Café"]]

// para imprimir o valor em determinada posição devemos chamra a variável entre () e em seguida entre [] a posição que queremos saber.
console.log(pessoa[0])

// todo array tem um tamanho. Este tem o tamanho de 5 elementos. Para sabermos este tamanho usamos console.log(pessoa.length)